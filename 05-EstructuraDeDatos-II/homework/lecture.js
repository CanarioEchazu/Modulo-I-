/*const { list  } = require("@11ty/eleventy/src/Util/AsyncEventEmitter");

function data 

list.prototype.add= function(data){
    var node = new Node(data),
    current = this.head
    //si esta vacia
    if (!current) {
        this.head = node;
        this._length++;
        return node;
    }
}

//si no esta vacia recorro hasta encontrar el último
while (current.next) {
    current = current.next;
    }
current.next = node;
this._length ++;
return node;

};
list.prototype.add = function(data)
*/

// as

